<UserControl x:Class="Presentation.WPF.Views.UserControls.ConfirmationDialog"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:sys="clr-namespace:System;assembly=mscorlib"
             mc:Ignorable="d" 
             d:DesignHeight="150" d:DesignWidth="300"
             Width="300">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Grid.Row="0" Margin="0,0,0,16">
            <materialDesign:PackIcon Kind="AlertCircleOutline" Foreground="{DynamicResource DangerBrush}" Width="24" Height="24" Margin="0,0,8,0" VerticalAlignment="Center"/>
            <TextBlock Text="Confirm Deletion" Style="{StaticResource MaterialDesignHeadline6TextBlock}" VerticalAlignment="Center"/>
        </StackPanel>

        <TextBlock Grid.Row="1" Text="Are you sure you want to delete this record? This action cannot be undone." 
                   TextWrapping="Wrap" Margin="0,0,0,24"
                   Style="{StaticResource MaterialDesignBody2TextBlock}"/>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Style="{StaticResource MaterialDesignFlatButton}" Content="NO"
                    IsCancel="True"
                    Command="{x:Static materialDesign:DialogHost.CloseDialogCommand}"
                    Margin="0,0,8,0"
                    Foreground="{DynamicResource MaterialDesignBody}">
                <Button.CommandParameter>
                    <sys:Boolean>False</sys:Boolean>
                </Button.CommandParameter>
            </Button>
            
            <Button Style="{StaticResource MaterialDesignFlatButton}" Content="YES"
                    IsDefault="True"
                    Command="{x:Static materialDesign:DialogHost.CloseDialogCommand}"
                    Background="{DynamicResource DangerBrush}"
                    Foreground="White">
                <Button.CommandParameter>
                    <sys:Boolean>True</sys:Boolean>
                </Button.CommandParameter>
            </Button>
        </StackPanel>
    </Grid>
</UserControl>
