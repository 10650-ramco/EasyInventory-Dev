<UserControl x:Class="Presentation.WPF.Views.InventoryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Presentation.WPF.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid x:Name="Inventory">
        <Grid.RowDefinitions>
            <RowDefinition Height="105"/>
            <RowDefinition Height="125"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Main Content Grid -->
        <Grid x:Name="MainContent" Grid.RowSpan="3">
            <Grid.RowDefinitions>
                <RowDefinition Height="105"/>
                <RowDefinition Height="125"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

        <!-- Section 1 -->
        <Border Grid.Row="0" Background="#F5F5F5" BorderBrush="#F0F0F0" BorderThickness="0,0,0,1" Padding="15">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- Header Row -->
                <Grid Grid.Row="0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <!-- Inventory Header -->
                    <TextBlock Text="Product" Foreground="Black" FontSize="20" FontWeight="Bold" VerticalAlignment="Center"/>
                </Grid>

                <!-- Search and Button Row -->
                <Grid Grid.Row="1" Margin="0,10,0,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="300"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <!-- Search Box with Icon -->
                    <Border Background="White" BorderBrush="#E8E8E8" BorderThickness="0.5" CornerRadius="5">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <!-- Search Icon -->
                            <TextBlock Grid.Column="0" 
                                       Text="🔍" 
                                       Foreground="#999999" 
                                       FontSize="16" 
                                       Margin="10,0,5,0"
                                       VerticalAlignment="Center"/>

                            <!-- Search TextBox -->
                            <Grid Grid.Column="1">
                                <TextBox BorderThickness="0" 
                                         Background="Transparent"
                                         Padding="5,8"
                                         FontSize="12"/>
                                <TextBlock Text="Search inventory..." 
                                           Foreground="#CCCCCC" 
                                           Padding="5,8" 
                                           FontSize="12"
                                           IsHitTestVisible="False"
                                           VerticalAlignment="Center"/>
                            </Grid>
                        </Grid>
                    </Border>

                    <!-- Spacer -->
                    <Border Grid.Column="1" Background="Transparent" CornerRadius="4" Padding="0" Margin="10,0,0,0" HorizontalAlignment="Right">
                        <Button Content="+ Add New Item" 
                                Background="#1976D2"
                                Foreground="White" 
                                Padding="12,8" 
                                FontSize="12"
                                FontWeight="Bold"
                                Cursor="Hand"
                                BorderThickness="0"
                                Click="AddNewItem_Click"/>
                    </Border>
                </Grid>
            </Grid>
        </Border>

        <!-- Section 2 -->
        <Border Grid.Row="1" Background="#FFFFFF" BorderBrush="#F0F0F0" BorderThickness="0,0,0,0">
            <Grid Margin="3">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <!-- Total Items Card -->
                <Border Grid.Column="0" Background="#E3F2FD" CornerRadius="8" Margin="5" Padding="15">
                    <StackPanel VerticalAlignment="Center">
                        <TextBlock Text="Total Items" Foreground="#1976D2" FontSize="12" FontWeight="Bold"/>
                        <TextBlock Text="1,250" Foreground="#1565C0" FontSize="24" FontWeight="Bold" Margin="0,5,0,0"/>
                    </StackPanel>
                </Border>

                <!-- Low Stock Card -->
                <Border Grid.Column="1" Background="#FFF3E0" CornerRadius="8" Margin="5" Padding="15">
                    <StackPanel VerticalAlignment="Center">
                        <TextBlock Text="Low Stock" Foreground="#F57C00" FontSize="12" FontWeight="Bold"/>
                        <TextBlock Text="45" Foreground="#E65100" FontSize="24" FontWeight="Bold" Margin="0,5,0,0"/>
                    </StackPanel>
                </Border>

                <!-- Out of Stock Card -->
                <Border Grid.Column="2" Background="#FFEBEE" CornerRadius="8" Margin="5" Padding="15">
                    <StackPanel VerticalAlignment="Center">
                        <TextBlock Text="Out of Stock" Foreground="#C62828" FontSize="12" FontWeight="Bold"/>
                        <TextBlock Text="12" Foreground="#B71C1C" FontSize="24" FontWeight="Bold" Margin="0,5,0,0"/>
                    </StackPanel>
                </Border>
                
                <!-- Out of Stock Card -->
                <Border Grid.Column="2" Background="#FFEBEE" CornerRadius="8" Margin="5" Padding="15">
                    <StackPanel VerticalAlignment="Center">
                        <TextBlock Text="Out of Stock" Foreground="#C62828" FontSize="12" FontWeight="Bold"/>
                        <TextBlock Text="12" Foreground="#B71C1C" FontSize="24" FontWeight="Bold" Margin="0,5,0,0"/>
                    </StackPanel>
                </Border>
                
                    <!-- Total Value Card -->
                <Border Grid.Column="3" Background="#E8F5E9" CornerRadius="8" Margin="5" Padding="15">
                    <StackPanel VerticalAlignment="Center">
                        <TextBlock Text="Total Value" Foreground="#388E3C" FontSize="12" FontWeight="Bold"/>
                        <TextBlock Text="$45,320" Foreground="#2E7D32" FontSize="24" FontWeight="Bold" Margin="0,5,0,0"/>
                    </StackPanel>
                </Border>
            </Grid>
        </Border>

        <!-- Section 3 -->
        <Border Grid.Row="2" Background="#FFFFFF" BorderBrush="#F0F0F0" BorderThickness="0.5" Padding="0">
            <DataGrid AutoGenerateColumns="False" 
                      BorderThickness="0"
                      Background="#FFFFFF"
                      Foreground="#333333"
                      GridLinesVisibility="None"
                      HeadersVisibility="Column"
                      RowBackground="#FFFFFF"
                      AlternatingRowBackground="#FAFAFA"
                      Margin="0"
                      VerticalGridLinesBrush="Transparent"
                      HorizontalGridLinesBrush="#F0F0F0"
                      RowHeight="35">
                <DataGrid.ColumnHeaderStyle>
                    <Style TargetType="DataGridColumnHeader">
                        <Setter Property="Background" Value="#F5F5F5"/>
                        <Setter Property="Foreground" Value="#333333"/>
                        <Setter Property="BorderThickness" Value="0,0,1,0"/>
                        <Setter Property="BorderBrush" Value="#E0E0E0"/>
                        <Setter Property="Padding" Value="8,10"/>
                        <Setter Property="FontWeight" Value="Bold"/>
                        <Setter Property="FontSize" Value="12"/>
                    </Style>
                </DataGrid.ColumnHeaderStyle>
                <DataGrid.RowStyle>
                    <Style TargetType="DataGridRow">
                        <Setter Property="BorderThickness" Value="0"/>
                        <Setter Property="Background" Value="Transparent"/>
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#F5F5F5"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter Property="Background" Value="#E3F2FD"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </DataGrid.RowStyle>
                <DataGrid.CellStyle>
                    <Style TargetType="DataGridCell">
                        <Setter Property="BorderBrush" Value="#F5F5F5"/>
                        <Setter Property="BorderThickness" Value="0,0,1,0"/>
                        <Setter Property="Padding" Value="8,0"/>
                    </Style>
                </DataGrid.CellStyle>
                <DataGrid.Columns>
                    <!-- Product Id -->
                    <DataGridTextColumn Header="Product ID" Binding="{Binding ProductId}" Width="80" IsReadOnly="True"/>
                    
                    <!-- Product Name -->
                    <DataGridTextColumn Header="Product Name" Binding="{Binding ProductName}" Width="150" IsReadOnly="True"/>
                    
                    <!-- Category -->
                    <DataGridTextColumn Header="Category" Binding="{Binding Category}" Width="100" IsReadOnly="True"/>
                    
                    <!-- Unit -->
                    <DataGridTextColumn Header="Unit" Binding="{Binding Unit}" Width="80" IsReadOnly="True"/>
                    
                    <!-- Price -->
                    <DataGridTextColumn Header="Price" Binding="{Binding Price, StringFormat='C'}" Width="80" IsReadOnly="True"/>
                    
                    <!-- Status -->
                    <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="100" IsReadOnly="True"/>
                    
                    <!-- Actions -->
                    <DataGridTemplateColumn Header="Actions" Width="120">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                    <!-- Edit Button -->
                                    <Button Content="✎" 
                                            Background="#4CAF50" 
                                            Foreground="White" 
                                            Padding="6,4" 
                                            Margin="2" 
                                            FontSize="12"
                                            Cursor="Hand"
                                            BorderThickness="0"/>
                                    
                                    <!-- Delete Button -->
                                    <Button Content="🗑" 
                                            Background="#F44336" 
                                            Foreground="White" 
                                            Padding="6,4" 
                                            Margin="2" 
                                            FontSize="12"
                                            Cursor="Hand"
                                            BorderThickness="0"/>
                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </Border>
        </Grid>


        <!-- Blur & Shadow Overlay -->
        <Grid x:Name="DrawerOverlay" Visibility="Collapsed" Grid.RowSpan="3" MouseDown="Overlay_MouseDown">
            <Border Background="#80000000" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
                <Border.Effect>
                    <BlurEffect Radius="5"/>
                </Border.Effect>
            </Border>
        </Grid>
        
        <!-- Side Drawer -->
        <!--<Border Grid.RowSpan="3" Background="#80000000" x:Name="DrawerOverlay" Visibility="Collapsed" MouseDown="Overlay_MouseDown"/>-->

        <Border Grid.RowSpan="3" 
                x:Name="SideDrawer"
                Width="400" 
                Background="#FFDDDDDD"
                HorizontalAlignment="Right"
                RenderTransformOrigin="0,0"
                Visibility="Collapsed">

            <!-- Shadow Effect -->
            <Border.Effect>
                <DropShadowEffect 
                    Color="Black" 
                    Direction="270"      
                    ShadowDepth="5"
                    BlurRadius="15"
                    Opacity="0.4"/>
            </Border.Effect>
                
            <Border.RenderTransform>
                <TranslateTransform x:Name="SideDrawerTransform" X="400"/>
            </Border.RenderTransform>
            
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- Header -->
                <Border Grid.Row="0" Background="#263238" BorderBrush="#F0F0F0" BorderThickness="0,0,0,1" Padding="20,15">
                    <TextBlock Text="Add New Item" Foreground="White" FontSize="16" FontWeight="Bold"/>
                </Border>

                <!-- Form Content -->
                <ScrollViewer Grid.Row="1" Padding="20">
                    <StackPanel>
                        <!-- Product Name -->
                        <StackPanel>
                            <TextBlock Text="Product Name" Foreground="#333333" FontSize="12" FontWeight="Bold" Margin="0,0,0,5"/>
                            <TextBox BorderBrush="#E0E0E0" BorderThickness="1" Padding="10,8" FontSize="12"/>
                        </StackPanel>

                        <!-- Category -->
                        <StackPanel>
                            <TextBlock Text="Category" Foreground="#333333" FontSize="12" FontWeight="Bold" Margin="0,0,0,5"/>
                            <TextBox BorderBrush="#E0E0E0" BorderThickness="1" Padding="10,8" FontSize="12"/>
                        </StackPanel>

                        <!-- Unit Type -->
                        <StackPanel>
                            <TextBlock Text="Unit Type" Foreground="#333333" FontSize="12" FontWeight="Bold" Margin="0,0,0,5"/>
                            <TextBox BorderBrush="#E0E0E0" BorderThickness="1" Padding="10,8" FontSize="12"/>
                        </StackPanel>

                        <!-- Price -->
                        <StackPanel>
                            <TextBlock Text="Price" Foreground="#333333" FontSize="12" FontWeight="Bold" Margin="0,0,0,5"/>
                            <TextBox BorderBrush="#E0E0E0" BorderThickness="1" Padding="10,8" FontSize="12"/>
                        </StackPanel>

                        <!-- Stock -->
                        <StackPanel>
                            <TextBlock Text="Stock" Foreground="#333333" FontSize="12" FontWeight="Bold" Margin="0,0,0,5"/>
                            <TextBox BorderBrush="#E0E0E0" BorderThickness="1" Padding="10,8" FontSize="12"/>
                        </StackPanel>

                        <!-- Low Stock -->
                        <StackPanel>
                            <TextBlock Text="Low Stock" Foreground="#333333" FontSize="12" FontWeight="Bold" Margin="0,0,0,5"/>
                            <TextBox BorderBrush="#E0E0E0" BorderThickness="1" Padding="10,8" FontSize="12"/>
                        </StackPanel>
                    </StackPanel>
                </ScrollViewer>

                <!-- Footer Buttons -->
                <Grid Grid.Row="2" Margin="20" Background="Transparent">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <!-- Cancel Button -->
                    <Button Grid.Column="0" 
                            Content="Cancel" 
                            Background="Transparent"
                            Foreground="#1976D2" 
                            BorderBrush="#1976D2"
                            BorderThickness="1"
                            Padding="10,8" 
                            FontSize="12"
                            FontWeight="Bold"
                            Margin="0,0,5,0"
                            Cursor="Hand"
                            Click="Cancel_Click"/>

                    <!-- Save Button -->
                    <Button Grid.Column="1" 
                            Content="Save" 
                            Background="#4CAF50"
                            Foreground="White" 
                            Padding="10,8" 
                            FontSize="12"
                            FontWeight="Bold"
                            Margin="5,0,0,0"
                            Cursor="Hand"
                            BorderThickness="0"
                            Click="Save_Click"/>
                </Grid>
            </Grid>
        </Border>
    </Grid>
</UserControl>
